import React from 'react';
import Base from '../core/Base';

const AdminDashboard = (props) =>  {
    return (
        <Base title="AdminDashboard Page">
            <h1>This is a AdminDashboard Page</h1>
        </Base>
    );
}

export default AdminDashboard;